# Contributing

이슈와 풀 리퀘스트를 환영합니다. 개발 환경 설정은 [HACKING.md](./HACKING.md)를 보세요.

## 특히 환영하는 기여

**계산이 틀린 사례.** 이 프로젝트에서 가장 값진 기여입니다. 다른 만세력과 결과가 다른 생년월일시를 찾으셨다면 이슈로 올려 주세요. 필요한 것은 다음과 같습니다.

- 생년월일시와 출생지
- 이 앱이 낸 결과 (명식 상단의 보정 표시줄 포함)
- 다르게 나온 곳과 그쪽 결과
- 가능하면 그쪽이 쓰는 관행 (진태양시 보정 방식, 자시 처리)

관행 차이로 판명되면 설정 옵션으로, 버그로 판명되면 테스트와 함께 수정합니다.

**해석 규칙.** `rules.json`의 내용을 다듬거나 추가하는 기여입니다. 톤 규약은 [docs/research/interpretation-content.md](./docs/research/interpretation-content.md)에 있습니다.

**출처 있는 검증 데이터.** 절기 시각, 공표된 명식 사례처럼 출처를 댈 수 있는 값이면 테스트로 편입합니다.

## 풀 리퀘스트

1. 브랜치를 따고 작업합니다. `main`에 직접 커밋하지 않습니다.
2. `cd SajuKit && swift test`가 통과해야 합니다.
3. 계산 동작을 바꾸면 테스트를 함께 냅니다.
4. 커밋 메시지는 [Conventional Commits](https://www.conventionalcommits.org/)를 따릅니다.

```
feat(engine): 균시차 보정 옵션 추가
fix(lunar): 윤달 경계에서 하루 어긋나는 문제 수정
docs(adr): 룰 인덱스 결정 기록
test(pillars): 1955년 서머타임 사례 추가
```

`feat:`와 `fix:`만 릴리스를 만듭니다. `chore:` `docs:` `refactor:` `test:` `ci:`는 만들지 않습니다.

## 계산 규칙을 바꿀 때

`PublishedCaseTests`와 `SolarTermReferenceTests`의 기댓값은 외부 공표 자료에서 왔습니다. **구현에 맞춰 기댓값을 고치지 마세요.** 기댓값이 틀렸다고 판단하면 새 출처를 `docs/research/`에 추가하고 그것을 근거로 바꿉니다.

유파에 따라 갈리는 지점이라면 어느 한쪽을 정답으로 만들지 말고 `SajuOptions`에 옵션을 추가하세요. 근거는 [ADR 0005](./docs/adr/0005-expose-school-differences-as-settings.md)에 있습니다.

## 구조를 바꿀 때

되돌리기 어려운 결정이라면 [ADR](./docs/adr/)을 함께 작성해 주세요. 형식은 가볍습니다. 맥락, 결정, 결과 세 절이면 됩니다.

`SajuKit`에 외부 의존성을 추가하는 제안은 먼저 이슈로 논의해 주세요. 의존성 없음이 이 패키지의 설계 전제입니다.

## 해석 콘텐츠를 쓸 때

- 존댓말을 씁니다.
- 단정하지 않습니다. "~한 편입니다", "~경향이 있습니다"를 씁니다.
- 운명론적 공포를 조장하지 않습니다.
- 의료·투자·법률 조언을 하지 않습니다.
- 이모지를 쓰지 않습니다. 한자는 괄호로 병기합니다.
- 일반론을 쓰지 않습니다. 각 항목은 그 간지나 십신 고유의 내용이어야 합니다.

## 행동 규범

상호 존중을 전제합니다. 명리 해석에는 견해차가 있을 수 있으며, 근거를 들어 논의하고 갈리는 지점은 옵션으로 남깁니다.
