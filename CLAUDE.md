# CLAUDE.md

먼저 [AGENTS.md](./AGENTS.md)를 읽으십시오. 넘지 말아야 할 선과 빌드 명령이 거기 있습니다.

이 문서는 그 위에 얹는 것입니다. 규칙이 아니라 **판단의 근거**입니다. 왜 이 저장소가 이런 모양인지, 새로운 결정을 내릴 때 어느 쪽으로 기울어야 하는지를 적었습니다. 여기 적힌 것들은 대부분 이 프로젝트를 만들면서 실제로 부딪혀 얻은 것이며, 몇 개는 제가 틀렸다가 고친 것입니다.

---

## 1. 요청 목록은 증상 목록이다

"⌘N을 누르면 새 창이 뜨면서 처음부터 다시 생성돼요"는 단축키 버그처럼 보입니다. 단축키를 바꾸면 그 증상은 사라집니다. 그런데 인물을 바꿨다 돌아와도 여전히 재생성되고, 중단·재개를 붙일 자리도 없습니다.

원인은 하나였습니다. **생성 상태가 뷰에 살고 있었습니다.** 세 가지 요청이 같은 뿌리에서 나왔고, 뿌리를 고치자 셋이 함께 해결됐습니다.

요청을 받으면 먼저 물으십시오. 이것들이 각각의 문제인가, 아니면 하나의 구조적 결함이 여러 표면으로 새어 나온 것인가.

"오늘의 운세"와 "캘린더"를 함께 요청받았을 때도 마찬가지였습니다. 두 기능이 아니라 **앱에 시간 축이 없다**는 신호였습니다. 명식은 평생 바뀌지 않으니 한 번 보고 닫는 화면이고, 그것만으로는 다시 열 이유가 없습니다. 시간 축은 기능 추가가 아니라 두 번째 사용 모드였습니다.

## 2. 쉬운 참여 유도가 정체성과 충돌하면 버린다

운세 캘린더를 만들면 길일과 흉일을 칠하고 싶어집니다. 한눈에 읽히고, 사용자도 그걸 기대하고, "오늘 조심하세요"는 강력한 훅입니다.

하지 않았습니다. 두 가지 이유입니다.

명리 이론상 근거가 약합니다. 충(沖)은 나쁜 것이 아니라 움직임이 생기는 국면이고, 사(死)와 묘(墓)는 불운이 아니라 기운의 세기와 방향을 나타내는 말입니다. "나쁜 날"로 옮기는 순간 원래 이론에서 멀어집니다.

제품으로서 자기모순입니다. 이 앱의 정체성은 모든 서술에 근거를 붙이는 것인데, 근거를 보여주면서 동시에 등급을 매기면 사용자는 근거가 아니라 색깔만 봅니다.

**대가는 치릅니다.** 한눈에 읽히는 재미가 줄어듭니다. 그래도 색 하나로 하루를 판정하는 앱은 이미 많습니다.

## 3. 옳은 선택을 싸게, 그른 선택을 비싸게 만든다

`DayReading`에는 점수 필드가 없습니다. 등급을 매기지 않겠다는 결정이 문서가 아니라 **자료구조** 수준에 박혀 있습니다. 나중에 누군가 길흉을 넣고 싶으면 타입부터 고쳐야 하고, 그러면서 왜 없었는지를 마주하게 됩니다.

원칙을 주석으로만 적으면 지워집니다. 타입으로 적으면 남습니다.

같은 이유로 시간운 콘텐츠에 판정 표현이 들어가지 않는지 **테스트가 검사**합니다.

## 4. 모델은 계산하지 않는다

이 저장소의 단일 최대 결정입니다. 상세는 [ADR 0002](./docs/adr/0002-separate-deterministic-engine-from-llm.md)에 있고, 여기서는 왜 이게 일반 원칙인지만 적습니다.

언어 모델은 **확정된 사실을 사람이 읽을 문장으로 옮기는 일**에 강하고, **값을 산출하는 일**에 약합니다. 유효 2B 모델은 60갑자 순환이나 절기 경계 비교를 안정적으로 못 합니다. 그럴듯하게 틀린 결과가 나오고, 사주 앱에서 명식이 틀리면 나머지는 전부 무의미합니다.

경계를 그으면 세 가지를 얻습니다.

- 작은 모델로 충분합니다. E2B 4-bit 3.6GB면 됩니다.
- 틀릴 수 없습니다. 계산은 테스트로 고정되어 있습니다.
- **모델이 선택 사항이 됩니다.** 규칙 엔진이 근거 원문을 그대로 조립하므로 첫 실행에서 3.6GB를 요구하지 않아도 됩니다.

세 번째가 특히 중요합니다. AI 기능을 붙일 때는 항상 물으십시오. **모델이 없으면 이 앱은 무엇인가.** 답이 "쓸모없음"이면 설계가 잘못된 것입니다.

## 5. 닫힌 온톨로지에는 벡터 검색을 쓰지 않는다

기본값처럼 여겨지는 방식은 RAG입니다. 문헌을 청크로 나눠 임베딩하고 유사도로 검색합니다.

사주의 온톨로지는 완전히 닫혀 있습니다. 천간 10, 지지 12, 십신 10, 오행 5. 조합은 유한하고 열거 가능합니다. 이런 영역에서 임베딩 유사도는 정확도를 **떨어뜨리기만** 합니다. "편재"를 찾아야 하는데 "정재" 청크가 딸려 옵니다.

그래서 태그 정확 매칭을 씁니다. 그리고 여기서 예상 못 한 것을 얻었습니다.

**어떤 문장이 어떤 규칙에서 나왔는지 항상 압니다.** UI의 근거 칩이 여기서 나왔고, 그게 이 앱의 가장 큰 차별점이 됐습니다. 정확도를 위해 고른 구조가 제품의 정체성을 만들었습니다.

구조적 선택의 부수 효과를 살피십시오. 때로 그게 본체보다 큽니다.

## 5-1. 질문을 라우팅하는 일도 닫힌 온톨로지다

상담 기능에서 사용자의 고민을 명리 축으로 옮길 때도 임베딩을 쓰지 않았습니다. **고민의 축도 닫혀 있습니다** — 십신 10개가 사람이 겪는 일의 범주를 이미 나눠 놓았기 때문입니다. 키워드 정확 매칭에 축 10개면 충분합니다.

라우팅이 실패하면 **사용자가 직접 고릅니다.** 짐작으로 고르면 엉뚱한 근거로 답하게 되고, 그 실패는 조용합니다 — 답이 그럴듯하게 나오므로 아무도 알아채지 못합니다. 빈 결과를 돌려주는 것이 정상적인 결과입니다.

## 6. "모른다"는 품질 기준이지 실패가 아니다

대화 기능의 시스템 지시에 이렇게 적었습니다.

> 근거에 없는 것을 물으면 아는 척하지 말고, 지금 정보로는 말씀드리기 어렵다고 솔직하게 답합니다. 이것이 지어내는 것보다 낫습니다.

추천 질문도 **실제로 근거가 있는 주제로만** 만듭니다. 답할 수 없는 질문을 권하는 것은 정직하지 않습니다.

LLM 기능을 설계할 때 "무엇을 답하게 할까"만큼 "무엇을 모른다고 말하게 할까"를 정하십시오.

## 6-1. 모델은 계산하지 않고, **판단하지도 않는다**

§4의 확장입니다. 상담 기능을 만들면서 경계를 한 칸 더 밀었습니다.

사용자가 자해나 자살을 언급했을 때 무엇을 할지 **모델에게 맡기지 않습니다.** 글은 모델에 닿기 전에 결정론적 선별을 통과하고, 걸리면 앱이 고정된 안내를 직접 보여주며 생성은 일어나지 않습니다.

근거는 OpenAI가 직접 밝힌 것입니다. **긴 대화에서는 안전 학습이 열화됩니다** — 처음에는 핫라인을 안내하다가 여러 턴 뒤에는 안전장치를 어기는 답을 내놓습니다. 계산을 맡기지 않는 이유와 정확히 같습니다. 그럴듯하게 틀리면 사람이 다칩니다.

일반화하면 이렇습니다. **틀렸을 때의 대가가 비대칭인 판단은 생성 경로 밖에 두십시오.**

## 6-2. 근거는 답 뒤가 아니라 답 앞에 둔다

Pew 조사에서 AI 요약이 있을 때 **인용 링크 클릭은 전체 방문의 1%** 수준입니다. 근거를 답변 아래에 달아 두는 것만으로는 읽히지 않습니다.

그래서 상담 화면은 **풀이를 받기 전에** 쓰일 근거를 헤더에 보여줍니다. 무엇을 근거로 답할지 미리 보는 것이 답을 받고 나서 검증하는 것보다 낫습니다.

또 하나. Tow Center는 AI 검색엔진의 **인용 오류율이 60% 이상**이라고 보고합니다. 이 오류는 **모델이 인용을 생성하기 때문에** 생깁니다. 이 저장소는 방향이 반대입니다 — 규칙을 먼저 고르고 그 규칙으로 답을 만들므로 인용과 답이 어긋날 구조가 없습니다.

**인용을 생성하게 하지 말고, 인용에서 생성하게 하십시오.**

## 6-3. 이탈을 막는 장치를 만들지 않는다

AI 컴패니언 앱을 측정한 연구에서, 사용자가 작별을 알릴 때 **37%의 경우에 감정 부하 메시지가 등장**합니다. 그것은 작별 후 참여를 최대 14배 늘리지만 **동인은 즐거움이 아니라 분노와 호기심**입니다.

이 앱은 상담을 닫을 때 아무 말도 붙이지 않습니다. 다시 오라고 하지 않고, 알림으로 부르지 않고, 스트릭을 세지 않습니다.

같은 조사에서 아첨(sycophancy)이 측정된 해악임을 확인했습니다 — 모델은 사람보다 약 50% 더 높은 비율로 사용자 행동을 긍정하며 **유해한 행동에도** 그렇습니다. 그런데 사용자는 아첨하는 AI를 더 선호하고 더 신뢰합니다. **사용자가 좋아하는 것과 사용자에게 좋은 것이 갈리는 지점**이 있고, 참여 지표는 그 차이를 보여주지 않습니다.

---

## 7. 화면 하나에 주 신호는 하나

캘린더 첫 판에서 명식과 접점이 있는 모든 날에 점을 찍었습니다. 35일 중 28일에 점이 찍혔습니다. **전부 표시하면 아무것도 표시하지 않은 것과 같습니다.**

육합과 형은 흔하고 작용도 완만하므로 상세 패널로 내리고, 격자에는 충·삼합·일간합만 남겼습니다. 13일로 줄자 신호가 살아났습니다.

밀집한 화면에서는 부차 정보를 지우는 게 아니라 **층위를 내리십시오**. 오행은 얇은 밑줄로, 상세 관계는 클릭했을 때로.

## 8. 기계가 한 일을 보여준다

명식 화면에는 적용된 보정이 항상 떠 있습니다.

```
진태양시 12:40   경도 −32분   서머타임 −60분   야자시
```

다른 만세력과 결과가 다를 때 이유를 즉시 알 수 있습니다. 해석의 각 섹션에는 근거 칩이 붙고, 누르면 그 문단을 만든 규칙 원문이 나옵니다.

**신뢰는 결과가 아니라 과정을 보여줄 때 생깁니다.** 특히 계산이 복잡하거나 AI가 개입한 화면에서 그렇습니다.

## 9. 의견이 갈리는 지점은 침묵하는 기본값이 아니라 설정이다

만세력에는 유파에 따라 답이 갈리는 지점이 넷 있습니다. 진태양시 보정 방식, 균시차, 자시 경계, 대운수 끝처리. 조사해 보니 국내 서비스들이 실제로 서로 다른 기본값을 씁니다.

하나를 골라 조용히 적용하면 사용자는 결과가 다른 이유를 알 수 없습니다. 명리를 아는 사용자에게는 신뢰를 잃는 지점입니다.

기본값은 정하되 **그것을 정답이라 부르지 않습니다.** 설정 화면의 설명문이 짧은 교육 자료 역할을 합니다.

도메인에 논쟁이 있으면 그것을 숨기지 말고 제품의 일부로 만드십시오.

## 10. 중단은 실패가 아니라 행위다

생성 중단 버튼을 "취소"로 만들면 사용자는 만든 것을 잃습니다. 이 앱에서는 중단해도 완성된 섹션은 남고, 이어가면 남은 것만 만듭니다. 중단 상태는 `ensure()`가 존중합니다 — 사용자가 멈춘 것을 앱이 마음대로 다시 시작하지 않습니다.

끊긴 섹션은 부분 문장을 **버리고 다시** 만듭니다. 문장 중간에서 이어붙이면 앞뒤가 어긋난 글이 나옵니다. 재개의 단위는 사용자가 이해할 수 있는 단위여야 합니다.

## 11. 계산은 공짜지만 생성은 아니다

명식 계산은 밀리초입니다. 뷰가 그릴 때마다 다시 해도 됩니다(그래도 캐시는 합니다).

LLM 생성은 수십 초에 배터리를 씁니다. **사용자가 시키지 않으면 재생성하지 않습니다.** `ensure()`는 캐시가 있으면 아무 일도 하지 않고, `regenerate()`는 사용자가 버튼을 눌러야만 불립니다.

비용이 비대칭인 두 연산을 같은 정책으로 다루지 마십시오.

## 12. 뷰 재생성을 견뎌야 하는 상태는 뷰 밖에 둔다

SwiftUI는 뷰를 자유롭게 다시 만듭니다. 시트를 띄우거나, 창을 열거나, 부모가 갱신되면 `@State`가 초기화됩니다.

만드는 데 비용이 든 것, 사용자가 잃으면 화날 것은 뷰 밖에 두십시오. 이 저장소에서는 `InterpretationStore`가 그 자리입니다.

## 12-1. 그리고 프로세스 밖에 둔다

뷰 밖으로 옮긴 것만으로는 부족했습니다. 30초와 배터리를 들여 만든 문장이 앱 종료로 사라지면 "사용자가 시키지 않으면 재생성하지 않는다"(§11)는 원칙이 무의미합니다. **종료가 곧 강제 전량 재생성**이기 때문입니다.

비용을 치른 것은 디스크에 남깁니다. 계산은 그럴 필요가 없습니다. 이 구분이 저장 대상을 정하는 기준입니다.

파일명에 `hashValue`를 쓰지 마십시오. Swift의 Hasher는 프로세스마다 시드가 달라서 다음 실행에 같은 키가 다른 파일이 됩니다 — 고치려던 증상을 그대로 재현합니다.

## 12-2. 같은 슬롯에 두 가지를 넣으면 하나를 켤 때 다른 하나가 지워진다

이 저장소에서 가장 비싸게 배운 것입니다.

규칙 엔진 문장과 Gemma 문장을 "같은 것의 두 버전"으로 모델링했습니다. 그래서 캐시 키에 엔진 종류가 들어갔고, **모델을 켜는 것이 캐시 전체 무효화와 같은 뜻**이 됐습니다. 사용자는 문장을 갈아치우라고 한 적이 없는데 앱은 빈 캐시를 발견하고 성실하게 채웠습니다.

두 층은 애초에 다른 것이었습니다.

| | 기준선 | AI 문장 |
|---|---|---|
| 정체 | 계산 결과의 일부 | 사용자가 주문한 산출물 |
| 비용 | 밀리초, 결정론적 | 수십 초, 배터리, 비결정적 |
| 존재 | 항상 | 만들었을 때만 |

층을 나누자 엔진이 키에서 빠졌고, 모델을 켜는 것이 키를 흔들 방법이 없어졌습니다. **자동 재생성이 정책이 아니라 구조 수준에서 불가능해졌습니다.**

값이 두 개인 것처럼 보이는 슬롯을 만나면 물으십시오. 이것들은 같은 것의 다른 값인가, 서로 다른 층인가. 비용이 다르면 대개 다른 층입니다.

## 12-3. 읽기는 앱이 알아서, 쓰기는 사용자만

`ensure()`("없으면 채운다")를 삭제했습니다. 가드를 추가하는 게 아니라 함수를 없앴습니다. 이름이 남아 있으면 다음 사람이 또 부릅니다.

`onAppear`는 `restore()`(디스크 읽기)만 부릅니다. 생성은 `Button` 안에만 있습니다. 이 경계를 `scripts/check_generation_policy.sh`가 검사하고 CI가 실행합니다.

검사는 실제로 잡는지 확인한 뒤에 커밋했습니다. **통과만 하는 검사는 검사가 아닙니다.**

## 12-4. 상태 이름이 세 가지를 뜻하면 사용자는 설정이 풀렸다고 읽는다

`isReady = (container != nil)` 하나로 "AI를 쓸 수 있는가"를 판단했습니다. 앱을 다시 켜면 파일도 선택도 그대로인데 메모리에만 없어서, 사용자에게는 "모델을 다시 설정하라"고 보였습니다.

세 층으로 나눠 말하십시오. **설치됨**(파일이 있다, 영구) · **쓰기로 함**(사용자가 골랐다, 영구) · **적재됨**(메모리에 있다, 세션). UI가 기능을 제시할 때 보는 것은 앞의 두 층입니다. 적재는 버튼을 누른 뒤의 첫 단계이지 사용자가 관리할 일이 아닙니다.

세션 수명 값으로 영구적 의사를 판단하고 있는지 살피십시오. 로그인 토큰, 캐시, 연결 상태에서 같은 실수가 납니다.

---

## 13. 색은 계산하는 것이지 눈으로 고르는 것이 아니다

오행 팔레트는 라이트/다크 각각 명도 밴드, 채도 하한, 색각 이상 분리도, 정상 시각 분리도, 표면 대비 검사를 통과한 값입니다. 눈으로 고른 첫 후보는 전부 실패했습니다. 특히 목(청록)과 화(주황)가 적록색약에서 ΔE 1.3까지 붙었습니다 — 눈으로는 절대 못 잡습니다.

색을 고른 뒤 검증하지 말고, **검증을 통과할 때까지 고르십시오.**

## 14. 기하학적 중심과 시각적 중심은 다르다

"아이콘이 오른쪽으로 치우쳤다"는 지적을 받고 픽셀을 측정했습니다. 좌 198px, 우 197px. 정중앙이었습니다.

진짜 원인은 일주 열을 감싼 주사 테두리가 시각 무게를 한쪽으로 쏠리게 한 것이었습니다. 테두리를 밑줄로 바꾸자 해결됐습니다. 그리고 그 과정에서 **로고 SVG는 진짜로 5.75px 밀려 있었다**는 걸 발견했습니다.

지적을 받으면 먼저 측정하십시오. 원인이 짐작과 다를 수 있고, 짐작하지 못한 곳에 진짜 버그가 있을 수 있습니다.

## 15. 아이콘은 이름을 담을 수 있으면 담는다

첫 아이콘은 막대 넷이었고, 분석 도구 아이콘으로 읽혔습니다. 어느 앱이든 될 수 있는 그림이었습니다.

네 기둥이 각각 천간과 지지 두 칸으로 서면 여덟 칸이 됩니다. 사주팔자이자 앱 이름(four-eight)입니다. 같은 도형인데 의미가 붙었습니다.

주제의 세계 안에서 형태를 찾으십시오. 밖에서 가져온 은유는 대개 아무 데나 쓸 수 있고, 아무 데나 쓸 수 있는 것은 기억되지 않습니다.

---

## 15-1. 배포는 되돌릴 수 없는 값을 먼저 정하는 일이다

릴리스 파이프라인에서 진짜 어려운 부분은 자동화가 아닙니다. **한 번 정하면 못 바꾸는 값을 알아보는 것**입니다.

이 저장소에는 셋이 있습니다. 업데이트 피드 주소는 배포된 앱에 박혀 나가므로 그 주소를 영원히 살려야 합니다. EdDSA 공개키는 Sparkle이 제거를 거부하므로 첫 릴리스에 없으면 이후 업데이트를 기존 사용자에게 밀어넣을 방법이 없습니다. appcast 항목은 구버전 사용자가 판단 근거로 삼으므로 append-only입니다.

기능은 나중에 고칠 수 있습니다. 이 값들은 못 고칩니다. **v0.1.0을 내기 전에 이것부터 확정하십시오.**

일반화하면 이렇습니다. 새 시스템을 설계할 때 "나중에 바꿀 수 있는 것"과 "한 번 나가면 끝인 것"을 먼저 분류하십시오. 후자에 시간을 몰아 쓰는 게 맞습니다.

## 15-2. 되돌릴 수 없는 것 앞에는 사람이 서는 관문을 둔다

태그를 밀면 빌드가 돌고 **초안** 릴리스가 만들어집니다. 거기서 멈춥니다. 사람이 내려받아 실행해 보고 발행 버튼을 눌러야 appcast가 살아납니다.

자동화를 한 단계 덜 한 것이 아닙니다. **자동화의 끝을 되돌릴 수 있는 지점에 둔 것**입니다. 발행 전이면 초안과 태그를 지우면 그만이고, 사용자에게는 아무것도 나가지 않았습니다.

1인 프로젝트일수록 이 관문이 필요합니다. 잘못 나간 것을 잡아 줄 사람이 없기 때문입니다.

## 15-3. 마찰을 숨기지 않는다

이 앱은 Apple 공증을 받지 않았습니다. 그래서 직접 내려받은 사용자는 "손상되었기 때문에 열 수 없습니다"라는 경고를 봅니다.

README에 그 문구를 **그대로 적었습니다.** "약간의 설정이 필요합니다" 같은 완곡어법을 쓰지 않았습니다. 사용자가 그 화면을 마주쳤을 때 "문서에서 본 그거다"라고 알아볼 수 있어야 하기 때문입니다. 놀라게 하는 것이 신뢰를 깎지, 미리 말하는 것이 깎지 않습니다.

같은 이유로 우회 절차를 정확히 씁니다. macOS 15부터 우클릭 후 열기는 삭제되었고, "그래도 열기" 버튼은 실행 실패 후 약 1시간만 보입니다. 옛 안내를 그대로 두면 사용자는 없는 버튼을 찾다가 포기합니다.

## 15-4. 사용자 모르게 스스로를 바꾸지 않는다

자동 업데이트를 붙일 때 기본값을 "확인은 자동, 설치는 승인"으로 두었습니다. 자동 설치 스위치는 아예 만들지 않았습니다.

생년월일시를 다루면서 "이 Mac을 벗어나지 않습니다"를 내세우는 앱이 사용자 모르게 바이너리를 갈아치우면 앞뒤가 맞지 않습니다. 프라이버시 주장은 데이터에만 적용되는 게 아닙니다.

여기에 이 앱 고유의 요구가 하나 더 있습니다. **만세력 계산 규칙이 바뀌면 사용자가 이미 본 명식이 달라집니다.** 그런 릴리스는 조용히 넘기면 안 됩니다. 사용자는 앱이 틀렸다고 생각하지, 업데이트로 나아졌다고 생각하지 않습니다. appcast 항목에 표시를 달아 앱이 설치 후 한 번 알립니다.

기능이 아니라 **결과가 바뀌는 업데이트**가 있는 제품이라면 같은 장치가 필요합니다.

## 15-5. 느리면 로그를 세어 본다

첫 릴리스가 16분 걸렸습니다. "의존성이 많아서 그렇겠지"로 넘길 수도 있었지만, 빌드 로그에서 아키텍처별 컴파일 작업 수를 세어 봤습니다.

```
193  normal arm64
169  normal x86_64
```

**절반이 동작하지도 않는 Intel 슬라이스를 만드는 데 쓰이고 있었습니다.** MLX는 Metal 기반이라 Apple Silicon 전용인데, Xcode는 Release 구성에서 유효한 아키텍처를 모두 빌드합니다. `ARCHS: arm64` 한 줄로 빌드 시간과 배포물 크기가 각각 절반이 됐습니다.

느린 것을 참기 전에 **무엇이 시간을 쓰는지 세어 보십시오.** 짐작은 대개 틀리고, 로그에는 답이 들어 있습니다.

## 15-6. 알리기 전에 사슬 전체를 검증한다

릴리스를 발행하고 "됐습니다"라고 말하기는 쉽습니다. 그런데 사용자에게 닿기까지 고리가 여럿입니다.

이번에 실제로 확인한 것들입니다. 하나라도 어긋나면 조용히 실패합니다.

- 배포 자산을 **직접 내려받아** 아키텍처·버전·서명·실행 확인
- appcast가 `application/xml`로 서빙되는지 (`text/plain`이면 Sparkle이 파싱하지 못합니다)
- **피드의 EdDSA 서명이 실제 자산과 맞는지** — 어긋나면 앱이 업데이트를 조용히 거부하고, 사용자는 새 버전이 있다는 사실조차 모릅니다
- `brew info`가 경고 없이 새 버전을 보여주는지

조용히 실패하는 고리를 특히 조심하십시오. 오류를 내지 않는 실패는 오래 방치됩니다.

## 16. 자신과 비교하는 검증은 검증이 아니다

절기 계산이 맞는지 확인하려고 절기 계산을 두 번 돌리는 것은 아무것도 증명하지 않습니다. 외부에 공표된 값 51건과 공개 사주 사례 7건을 골든 데이터로 확보해 테스트로 고정했습니다.

**그리고 그 기댓값은 구현에 맞춰 고치지 않습니다.** 테스트가 깨지면 구현이 틀린 것입니다. 기댓값이 틀렸다고 판단되면 새 출처를 `docs/research/`에 추가하고 그 근거로 바꿉니다.

경계 사례를 일부러 모으십시오. UTC+8:30 시대에 서머타임이 겹친 1955년, 절입 2분 뒤 출생, 자정 전후로 갈리는 쌍, 해외 출생. 여기서 통과하면 일반 사례는 따라옵니다.

## 17. 함정은 테스트로 박아 둔다

1961년 입춘은 역서에 09:53으로 실려 있습니다. 당시 표준시가 UTC+8:30이었기 때문입니다. UTC+9로 읽으면 30분이 통째로 어긋납니다.

이건 주석으로 적어도 다음 사람이 또 밟습니다. **테스트로 만들면 못 밟습니다.** 테스트 이름과 주석에 왜 이 케이스가 존재하는지를 적었습니다.

## 18. 스크린샷은 빌드 산출물이다

`screencapture`는 화면 기록 권한을 요구하고 창 상태에 따라 결과가 흔들립니다. 대신 `NSWindow`에 뷰를 올리고 `cacheDisplay`로 뷰 계층을 직접 비트맵으로 뜹니다. 권한이 필요 없고 같은 입력에 같은 이미지가 나옵니다.

부수 효과가 컸습니다. **스크린샷이 실제 UI 버그를 잡아 줬습니다.** 해석 텍스트가 두 번 이어붙는 경쟁 조건과 "2,003년"이라는 로케일 버그가 눈으로 발견됐습니다. 코드만 보고 있으면 안 보입니다.

(SwiftUI `ImageRenderer`는 못 씁니다. `List`·`ScrollView`처럼 AppKit이 뒤를 받치는 뷰가 빈 자리로 나옵니다.)

## 19. 생성물은 커밋하고 드리프트를 검사한다

VSOP87 Swift 소스는 생성물입니다. 커밋하고, CI가 재생성해서 달라지면 실패시킵니다. 생성기를 고쳤는데 결과를 갱신하지 않는 사고를 막습니다.

절단 임계값을 바꾸면 버린 진폭의 합이 보고됩니다. **오차 예산을 감사할 수 있게 만드십시오.** 숫자를 조용히 바꾸는 생성기는 위험합니다.

---

## 20. AGENTS.md에는 명령이 아니라 넘지 말아야 할 선을 적는다

빌드 명령은 검색하면 나옵니다. 에이전트가 스스로 재구성할 수 없는 것은 **왜 이렇게 되어 있는가**입니다.

`SajuKit`에 의존성을 추가하지 말 것. 모델에게 계산을 시키지 말 것. 테스트 기댓값을 구현에 맞추지 말 것. 유파가 갈리는 지점에 정답을 정하지 말 것.

이런 것들이 없으면 에이전트는 합리적으로 보이는 방식으로 설계를 무너뜨립니다.

## 20-1. 로컬 통과가 CI 통과를 뜻하지 않는다

이 저장소에서 실제로 겪었습니다. 로컬 Xcode 26.2에서는 통과한 코드가 CI의 26.5에서 동시성 오류로 막혔습니다. 컴파일러가 판을 거듭할수록 엄격해지기 때문입니다.

두 가지를 지키십시오. **CI를 최신에 두십시오** — 로컬에 맞춰 낮추면 문제를 미루기만 합니다. 그리고 **엄격한 쪽이 옳다고 가정하십시오.** 실제로 그 오류는 진짜 문제를 가리키고 있었고, 고치면서 코드가 더 단순해졌습니다(`clear()`를 부르는 대신 세션을 다시 만들었습니다).

## 21. 주석은 무엇이 아니라 왜를 적는다

```swift
// 명시적 캡처 목록으로 session을 넘기면 non-Sendable 값이 격리
// 경계를 넘는 것으로 잡힌다. 암묵 캡처는 이 Task가 @MainActor
// 격리를 물려받으므로 안전하다.
```

코드가 무엇을 하는지는 코드가 말합니다. **왜 이 형태여야만 하는지**는 코드가 말하지 못합니다. 되돌리면 깨지는 것에는 이유를 적으십시오.

## 22. 조사 노트의 불일치는 지우지 않는다

`docs/research/`의 조사 노트는 자료 간 불일치를 그대로 보존합니다. 2003년 입춘 시각의 오류, 역서와 DE441의 1분 차, 위키백과의 2026년 입춘 표기 불일치.

정리해서 하나로 만들고 싶은 충동이 듭니다. 참으십시오. **불일치가 있었다는 사실이 가장 값진 정보입니다.** 나중에 테스트가 깨졌을 때 "이건 알려진 1분 차이"인지 "진짜 버그"인지 판단할 근거가 됩니다.

## 23. 조사와 결정을 분리한다

`docs/research/`는 "무엇이 사실인가"를, `docs/adr/`은 "그래서 무엇을 하기로 했는가"를 다룹니다.

사실은 바뀝니다. 패키지 버전이 오르고 라이선스가 정정됩니다. 결정은 그보다 오래 갑니다. 둘을 한 문서에 섞으면 사실을 갱신할 때마다 결정을 다시 써야 합니다.

## 24. 병렬로 돌릴 것과 직접 할 것을 나눈다

이 저장소를 만들면서 리서치 4건과 콘텐츠 집필 2건을 서브에이전트에게 맡기고, 아키텍처와 구현은 직접 했습니다.

**맡기기 좋은 것**: 출처를 모으는 조사, 규약이 명확한 콘텐츠 집필, 넓게 훑는 탐색. 결과를 검수할 기준이 명확한 일입니다.

**직접 할 것**: 경계를 긋는 결정, 자료구조 설계, 트레이드오프 판단. 위임하면 맥락이 잘려 일관성이 깨집니다.

맡길 때는 **판정 기준을 함께 주십시오.** "톤 규약을 지킬 것"이 아니라 "존댓말, 단정 금지, 의료 조언 금지, 이모지 금지, 항목마다 고유 내용"처럼. 그리고 결과를 반드시 검수하십시오 — 이번에도 서브에이전트가 만든 콘텐츠에서 제 테스트가 오탐을 냈고(정상 문장을 벌주는 검사였습니다), 고친 것은 콘텐츠가 아니라 테스트였습니다.

조사를 맡길 때 특히 효과가 컸던 지시가 둘 있습니다.

**"그래서 이렇게 하자는 제안을 쓰지 말 것."** 사실만 받으면 결정이 제 것으로 남고, 조사 노트가 §23의 역할(사실 보관)을 그대로 합니다. 제안을 섞어 받으면 그 제안에 이미 기울어진 채로 읽게 됩니다.

**"자료가 어긋나면 정리하지 말고 둘 다 기록할 것."** 이번 조사에서 이 지시가 실제 오류를 막았습니다. 한국 자살예방 상담전화 번호가 자료마다 달랐습니다(한쪽은 통합번호 109만, 공식 재단 사이트는 1393·1577-0199만). 에이전트가 "정리해서" 하나만 가져왔다면 **연결되지 않을 수 있는 번호를 앱에 박았을 것**입니다. 지금은 셋을 함께 적고 왜 그런지도 적어 두었습니다.

---

## 25. 실제로 밟은 지뢰

다음 사람이 다시 밟지 않도록 적어 둡니다.

| 증상 | 원인 |
|---|---|
| 위기 표현 검사가 투자 질문에 걸림 | 부분 문자열 검사가 낱말 경계를 넘음. "목을 매"가 "이 종**목을 매**수해도"에 걸린다. 어미까지 고정할 것 |
| 이름에 안전 검사가 걸림 | "유서"가 유서희·유서연 같은 이름에 걸린다. "유서를 쓰"처럼 붙여 쓸 것 |
| 중단 후 이어가면 답이 이상해짐 | MLX PR #414 — 잘린 턴이 종결자 없이 KV 캐시에 남고 Gemma가 특히 영향받음. 턴마다 세션을 새로 만들 것 |
| 모델을 고르자 해석이 전량 재생성됨 | 캐시 키에 엔진 종류가 들어 있어 모델 적재가 키를 바꿈. 엔진은 키가 아니라 `provenance`에 기록할 것 |
| 껐다 켜니 만든 문장이 사라짐 | 문서가 메모리에만 있었음. 비용을 치른 산출물은 디스크에 |
| 껐다 켜니 모델을 다시 설정하라고 함 | 세션 수명 값(`container`)으로 영구적 의사를 판단. 세 층을 나눠 볼 것 |
| 다음 실행에서 보관 파일을 못 찾음 | 파일명에 `hashValue` 사용. Hasher는 프로세스마다 시드가 다름. SHA-256을 쓸 것 |
| ⌘N에 새 창이 열림 | SwiftUI `WindowGroup` 기본 ⌘N. `CommandGroup(replacing: .newItem)`으로 대체 |
| 같은 섹션이 두 번 이어붙음 | 실행 중 판정을 `phase`로 함. `phase`는 첫 청크가 와야 `.running`이 됨. `task` 유무로 판정할 것 |
| "2,003년"으로 표시됨 | 한국어 로케일에서 Int 보간에 천 단위 구분자가 붙음. 연도는 `String()`으로 감쌀 것 |
| `ImageRenderer` 결과가 빈 화면 | `List`·`ScrollView` 등 AppKit 기반 뷰는 렌더링되지 않음 |
| 캡처 종료 시 세그폴트 | `close()`가 해제 애니메이션을 태움. `orderOut` 후 잠시 뒤 종료할 것 |
| 샌드박스에서 파일이 안 써짐 | 절대 경로 쓰기가 컨테이너로 리디렉션되거나 거부됨. 컨테이너에 쓰고 스크립트가 꺼내 갈 것 |
| 로컬은 되는데 CI만 깨짐 | Xcode 버전 차이(로컬 26.2 / CI 26.5). 동시성 검사 강도가 다름 |
| `sending value of non-Sendable type` | `ChatSession` 같은 non-Sendable 값에 격리 밖 `async` 메서드를 부르면 안 됨. 상태를 지우려면 메서드를 부르지 말고 **객체를 다시 만들 것** |
| Sparkle이 로드되지 않음 | ad-hoc 서명 + Hardened Runtime(Library Validation) 조합. 공증하지 않는 동안은 Hardened Runtime을 끌 것 |
| "우클릭 → 열기"가 안 됨 | macOS 15에서 삭제됨. 시스템 설정 → 개인정보 보호 및 보안 경로만 남음. 그 버튼도 실행 실패 후 약 1시간만 보임 |
| `brew install --cask` 후에도 Gatekeeper 경고 | Homebrew는 격리를 **부여**한다. 자체 tap의 `postflight`에서 `xattr -dr com.apple.quarantine`으로 제거해야 함 |
| 공식 homebrew-cask 등재 거부 | audit이 `gktool scan`으로 공증을 강제. 미공증 앱은 자체 tap만 가능 |
| `depends_on macos: ">= :sequoia"` 경고 | Homebrew 6에서 문자열 비교 형태는 deprecated. `depends_on macos: :sequoia` |
| 업데이트가 조용히 무시됨 | `CFBundleVersion`이 이전보다 낮음. Sparkle은 다운그레이드를 무시하며 오류도 내지 않음 |
| 릴리스 빌드가 두 배로 오래 걸림 | Xcode가 x86_64까지 빌드. Apple Silicon 전용 의존성이 있으면 `ARCHS: arm64`로 고정할 것 |
| `xcodebuild` 매크로/플러그인 실패 | `-skipMacroValidation -skipPackagePluginValidation` 둘 다 필요 |

---

## 26. 마지막으로

이 저장소에서 가장 어려운 부분은 LLM이 아니었습니다. Gemma를 MLX로 띄우는 것은 며칠짜리 작업이고 레퍼런스도 있습니다.

진짜 난이도는 두 곳에 있었습니다. **서머타임과 표준시 연혁과 진태양시가 뒤엉킨 경계 사례**, 그리고 **해석 콘텐츠의 품질**입니다. 후자는 코딩이 아니라 집필이고, 전체 공수의 상당 부분을 차지합니다.

기능을 더하기 전에 물으십시오. 이 앱이 잘하는 것이 더 잘해지는가, 아니면 잘하는 것이 하나 더 늘어나는가. 후자는 대개 둘 다 못하게 되는 길입니다.
